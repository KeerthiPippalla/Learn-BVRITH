<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mad1">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/LearnBvrith"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD1"
        tools:targetApi="31">
        <activity
            android:name=".AdminActivity"
            android:exported="false" />
        <!-- LoginActivity is set as the launcher activity -->

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity is declared but not exported, so it's not directly accessible -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Other activities -->
        <activity
            android:name=".NotifymeActivity"
            android:exported="false" />
        <activity
            android:name=".HiringChallengesActivity"
            android:exported="false" />
        <activity
            android:name=".FreeCoursesActivity"
            android:exported="false" />
        <activity
            android:name=".HackActivity"
            android:exported="false" />
        <activity
            android:name=".ContestActivity"
            android:exported="false" />
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
    </application>

</manifest>